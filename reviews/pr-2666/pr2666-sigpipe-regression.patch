diff --git a/tools/ci/tests/test_sbt_retry.py b/tools/ci/tests/test_sbt_retry.py
index 80ff8cdef6..f0778b779d 100644
--- a/tools/ci/tests/test_sbt_retry.py
+++ b/tools/ci/tests/test_sbt_retry.py
@@ -459,9 +459,9 @@ def test_probe_reports_maven_central_status_on_resolution_failure(tmp_path):
     probed = (tmp_path / "curl_log").read_text().strip().splitlines()
     assert probed == [
         "https://repo1.maven.org/maven2/com/globalmentor/"
-        "hadoop-bare-naked-local-fs/0.1.0/hadoop-bare-naked-local-fs-0.1.0.pom",
+        + "hadoop-bare-naked-local-fs/0.1.0/hadoop-bare-naked-local-fs-0.1.0.pom",
         "https://repo.maven.apache.org/maven2/com/globalmentor/"
-        "hadoop-bare-naked-local-fs/0.1.0/hadoop-bare-naked-local-fs-0.1.0.pom",
+        + "hadoop-bare-naked-local-fs/0.1.0/hadoop-bare-naked-local-fs-0.1.0.pom",
     ], probed
     curl_args = (tmp_path / "curl_args").read_text().strip().splitlines()
     assert len(curl_args) == 2
@@ -551,3 +551,33 @@ def test_evicted_entry_contents_are_logged(tmp_path):
     r = _run(tmp_path, "setup", sbt_cmd=sbt, env_overrides=env)
     assert r.returncode == 0, r.stdout + r.stderr
     assert "ivydata-0.1.0.properties" in (r.stdout + r.stderr)
+
+
+def test_truncated_find_listing_does_not_run_ls_fallback(tmp_path):
+    """SIGPIPE from head truncation means find worked; it is not a fallback case."""
+    env, _, _, _ = _ivy_layout(tmp_path)
+    fake_bin = tmp_path / "bin"
+    fake_bin.mkdir()
+    _write_exec(
+        fake_bin / "find",
+        """#!/usr/bin/env bash
+for i in {1..25}; do
+  echo "fake find entry $i"
+done
+exit 141
+""",
+    )
+    ls_calls = tmp_path / "ls_calls"
+    _write_exec(
+        fake_bin / "ls",
+        f"""#!/usr/bin/env bash
+echo called >> "{ls_calls}"
+exit 0
+""",
+    )
+    env = dict(env)
+    env["PATH"] = f"{fake_bin}{os.pathsep}{os.environ['PATH']}"
+    sbt = _fake_sbt_unresolved(tmp_path, fail_count=1)
+    r = _run(tmp_path, "setup", sbt_cmd=sbt, env_overrides=env)
+    assert r.returncode == 0, r.stdout + r.stderr
+    assert not ls_calls.exists(), "SIGPIPE 141 must not trigger the ls fallback"
